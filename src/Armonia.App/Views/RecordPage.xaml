<Page x:Class="Armonia.App.Views.RecordPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="RecordPage">

    <Grid Margin="20">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="🎙️ Record Audio" FontSize="24" Margin="0,0,0,12" TextAlignment="Center"/>
            <ProgressBar x:Name="AudioLevelBar" Width="300" Height="10" Minimum="0" Maximum="1" Margin="0,0,0,8"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button x:Name="StartButton" Content="Start Recording" Click="StartButton_Click" Margin="0,0,8,0"/>
                <Button x:Name="StopButton" Content="Stop Recording" Click="StopButton_Click" IsEnabled="False"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
